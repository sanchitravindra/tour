<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".startscreen"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

            <intent-filter>

                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <activity android:name=".Summerdestinations" />
        <activity android:name=".hawaii" />
        <activity android:name=".springdestination" />
        <activity android:name=".yoshemite" />
        <activity android:name=".winter" />
        <activity android:name=".winter_austria" />
        <activity android:name=".rainy" />
        <activity android:name=".rain_destination_1" />
    </application>

</manifest>